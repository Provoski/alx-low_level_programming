#include "main.h"

/**
 * _isupper - check if alphabet is upper or lower case
 * @c: variable of type int
 * Return: 1 if is upper or 0 if not
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
