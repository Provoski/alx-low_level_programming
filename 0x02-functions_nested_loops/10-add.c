#include <stdio.h>
#include "main.h"
/**
 * add - add two integer
 * @a: first integer
 * @b: second integer
 * Return: addintion of the integers
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
