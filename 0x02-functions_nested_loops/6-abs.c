#include "main.h"
#include <stdio.h>
/**
 * _abs - return absolute value of integer
 * @x: integer variable
 * Return: absolute value
 */
int _abs(int x)
{
	if (x < 0)
	{
		return (x * -1);
	}
	else
	return (x);
}
