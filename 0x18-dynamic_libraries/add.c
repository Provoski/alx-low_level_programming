#include <stdio.h>
/**
 * add - add two numbers
 * @a: variable for first number
 * @b: variable for second number
 * Return: addition of a and b
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}

