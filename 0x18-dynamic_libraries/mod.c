#include <stdio.h>
/**
 * mod - finds the division remainder of a and b
 * @a: variable for first number
 * @b: variable for second number
 * Return: remainder of a and b
 */
int mod(int a, int b)
{
	int result;

	result = a % b;
	return (result);
}

