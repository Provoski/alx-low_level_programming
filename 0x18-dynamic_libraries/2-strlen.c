#include "main.h"
#include <string.h>

/**
 * _strlen - calculate the lenght of a string
 * @s: variable
 * Return: Non
 */

int _strlen(char *s)
{
	int lenght;

	lenght = strlen(s);
	return (lenght);
}
