#include <stdio.h>

/**
 * op_add - add two numbers
 * @a: variable for first number
 * @b: variable of seconf number
 * Return: sum of numbers
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - suntract two numbers
 * @a: variable for first number
 * @b: variable of seconf number
 * Return: subtraction of numbers
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multiply two numbers
 * @a: variable for first number
 * @b: variable of seconf number
 * Return: multiplication of numbers
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - divide two numbers
 * @a: variable for first number
 * @b: variable of seconf number
 * Return: division value of numbers
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - find the remender of two number devision
 * @a: variable for first number
 * @b: variable of seconf number
 * Return: division value of numbers
 */
int op_mod(int a, int b)
{
	return (a % b);
}
