#include "main.h"

/**
 * main - master function
 * Description: entry point for other function
 * Return: always 0
 */

int main(void)
{
	int nb;

	nb = _atoi("98");
	printf("%d\n", nb);

	return (0);
}
