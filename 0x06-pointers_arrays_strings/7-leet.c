#include "main.h"
/**
 * leet - encode strings into 1337
 * @s: string variable
 * Return: encoded string
 */
char *leet(char *s)
{
	int i;
	char leet[] = {'a','e','o','t','l','A','E','O','T','L'};
	int sub[] = {4, 3, 0, 7, 0};

	for (i = 0; s[i] != '\0'; i++)
	{
		
	}
	return (s);
}
